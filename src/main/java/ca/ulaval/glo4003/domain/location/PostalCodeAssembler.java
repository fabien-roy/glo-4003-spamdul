package ca.ulaval.glo4003.domain.location;

public class PostalCodeAssembler {
  public PostalCode assemble(String postalCode) {
    // TODO : PostalCodeAssembler::assemble
    return new PostalCode(postalCode);
  }
}
